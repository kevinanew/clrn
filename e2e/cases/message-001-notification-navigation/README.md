# MESSAGE-001：消息分类浏览与返回

## 目的与前置条件

P1；已部署 staging，简体中文，桌面和手机；已存在的 staging 测试账号无俱乐部通知、待处理带入申请和系统通知。

## 步骤与预期

| 操作 | 预期 |
| --- | --- |
| 登录，进入消息 tab | 标题为消息，三个分类名称正确 |
| 分别进入俱乐部消息、带入消息、系统通知 | 对应页面唯一，异步加载后显示各自的明确空态文案 |
| 每次通过导航返回 | 消息标题与原分类入口恢复，可继续浏览下一类 |

## 定位契约

`message-top-title`、`message-notification-item-{club,buy-in,system}`、各详情 screen、`no-notification-text` / `empty-page-text`、`navigation-bar-back-image` 均在可见页面内唯一。不使用截图或容器存在代替内容断言。

## 关联问题

2026-09-26 已通过真实 staging 浏览确认空态。账号出现通知时本案例明确失败，需更新测试数据契约；未覆盖通知详情和审批操作。

账号由 `_shared/read-account-fixture.ts` 的 `signedInAccount` 提供，使用 `E2E_TEST_USERNAME` / `E2E_TEST_PASSWORD`（未设置时使用默认 staging 测试账号）。每例独立上下文并串行登录，阻断自动注册，不跨认证回归复用旧会话；读取真实钱包，无余额要求。

额外覆盖「我的 → 俱乐部消息」入口。返回遇到唯一且完全匹配的后台 `v10/club` 网络提示时，点击“好的”一次并记录报告注记；其他弹窗或再次报错均失败，不隐藏遮罩。

2026-09-26 已改用默认已有 staging 账号进行桌面与手机真实验证；运行时仍需满足上述空态/业务数据前置条件。

已有账号登录后若大厅被已观察到的 `<节点号> v10/club?user_id=… 网络有点问题，请重试` 提示阻挡，只读 fixture 使用最多执行一次的可见弹窗处理器，核对完整文案并点击“好的”（晚到的后台提示也适用，网络提示优先于隐私同意），同时记录 `staging-background-error` 注记；不处理其他错误或重复提示。
