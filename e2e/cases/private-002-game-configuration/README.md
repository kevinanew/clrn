# private-002：三种私人牌局配置与返回

## 目的与前置条件

P1，staging 已登录测试账号、空私人房间、简体中文，桌面与手机。无钻石余额要求，不提交创建。

## 步骤与预期

| 操作 | 预期 |
| --- | --- |
| 私人房间选择经典德州、拼三张或短牌 | 分别进入对应真实建局表单，展示玩法专属配置 |
| 修改牌局名称 | 输入值正确保存到当前表单 |
| 查看价格并打开高级设置 | 记分牌结算、10钻费用、默认30秒回合时长 |
| 返回私人房间 | 仍为空房，保留创建入口，钱包余额未变化 |

## 定位契约

当前页面的 `game-type-button-{type}`、`base-create-room-screen`、玩法规则标题、`room-name-input`、`advanced-options-button`、`operation-duration-title-text`、`navigation-bar-back-image` 唯一；历史隐藏页面不计入。

## 关联问题

2026-09-26 真实探索三个玩法与高级设置。实际创建、解散与退款由 private-001 覆盖经典德州；此案例覆盖另外两种玩法的配置入口与安全返回，不能等同于所有玩法的对局覆盖。

账号由 `_shared/read-account-fixture.ts` 的 `signedInAccount` 提供，使用 `E2E_TEST_USERNAME` / `E2E_TEST_PASSWORD`（未设置时使用默认 staging 测试账号）。每例独立上下文并串行登录，阻断自动注册，不跨认证回归复用旧会话；读取真实钱包，无余额要求。

2026-09-26 已改用默认已有 staging 账号进行桌面与手机真实验证；运行时仍需满足上述空态/业务数据前置条件。

已有账号登录后若大厅被已观察到的 `<节点号> v10/club?user_id=… 网络有点问题，请重试` 提示阻挡，只读 fixture 使用最多执行一次的可见弹窗处理器，核对完整文案并点击“好的”（晚到的后台提示也适用，网络提示优先于隐私同意），同时记录 `staging-background-error` 注记；不处理其他错误或重复提示。
